{
  "name": "resound-studio",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @resound-studio/web dev",
    "dev:api": "cd apps/api && python -m uvicorn main:app --reload --port 8000",
    "build": "pnpm --filter @resound-studio/web build",
    "lint": "pnpm --filter @resound-studio/web lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
